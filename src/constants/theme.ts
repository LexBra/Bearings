/**
 * @deprecated Prefer `import { theme } from "@/theme"`.
 * Kept so older imports keep working while we standardize on src/theme.
 */
export { theme, colors, spacing, radii, shadows } from "@/theme";
